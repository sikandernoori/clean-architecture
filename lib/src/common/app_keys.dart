import 'package:flutter/material.dart';

class AppKeys {
  static const ValueKey incrementButton = ValueKey('Increment button');
  static const ValueKey decrementButton = ValueKey('Decrement button');
  static const ValueKey loadingWidget = ValueKey('Loading widget');
  static const ValueKey errorWidget = ValueKey('Error widget');
  static const ValueKey counterWidget = ValueKey('Counter widget');
}
